// cn() class-name utility (scaffold placeholder).
// Will be implemented with clsx + tailwind-merge:
//   export const cn = (...inputs) => twMerge(clsx(inputs))
// NOTE: replaces the current `cn` npm package in package.json during the build phase.
export {}
