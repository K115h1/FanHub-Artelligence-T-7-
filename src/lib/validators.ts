// Validators (scaffold placeholder).
// Planned: isValidEmail(), password strength rules, required-field checks,
// file type/size validation for uploads.
// Rule: never trust user input — validate on the client AND server.
export {}
