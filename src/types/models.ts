// Shared domain models (scaffold placeholder).
// These mirror the MySQL tables 1:1 (see database/01_schema.sql) and the
// C# entities in FanHubPlus.Domain — one contract across all three tiers.
//
// Planned types: User, Role, Category, Content, MediaRating, CharacterProfile,
// MerchandiseItem, UpcomingRelease, Bookmark, Feedback, FanEvent,
// FanSubmission, ActivityLog, ChatbotQuery.
//
// Naming per rubric: interfaces/types are PascalCase with NO "I" prefix.
export {}
