// API contracts (scaffold placeholder).
// Planned types: ApiResponse<T>, Paginated<T> { items, page, totalPages, total },
// ApiError { statusCode, message, errors? }.
// Every service returns these — pages never see raw fetch responses.
export {}
