// upload.service (scaffold placeholder) — Data layer for file uploads.
// Planned functions: uploadAvatar(file), uploadFanContent(file).
// Must enforce client-side type/size validation; server re-validates as well.
export {}
