// event.service (scaffold placeholder) — Data layer for Location-Aware Event
// Discovery and Calendar.
// Planned functions: getEvents({ city?, lat?, lng?, radius?, month? }),
// getEventById(id). Uses geolocation from src/hooks/useGeolocation.ts.
export {}
