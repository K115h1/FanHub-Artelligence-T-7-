// auth.service (scaffold placeholder) — Data layer for authentication.
// Planned functions: register(), login(), logout(), refreshSession(),
// forgotPassword(email), resetPassword(token, newPassword), verifyEmail(token),
// getProfile(), updateProfile(), uploadAvatar(file).
export {}
