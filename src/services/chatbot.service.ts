// chatbot.service (scaffold placeholder) — DEFERRED until all mandatory
// requirements are complete (SRS marks the AI chatbot as optional).
// Planned functions: askQuestion(message), getChatHistory().
// Backend: ChatbotController + chatbot_queries table (see database/01_schema.sql).
export {}
