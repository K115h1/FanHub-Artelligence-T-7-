// content.service (scaffold placeholder) — Data layer for the Content Explorer.
// Planned functions: getContents(filters, sort, page) — one request for a full
// page of data (avoid N+1 style refetching), getContentById(id),
// rateContent(id, rating), incrementView(id) handled server-side.
export {}
