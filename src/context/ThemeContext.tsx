// ThemeContext (scaffold placeholder).
// Carries: theme ('light' | 'dark'), fontScale (1.0 | 1.15 | 1.3), setTheme(),
// setFontScale(). Backs the SRS dark-mode toggle and font-size adjustment.
export {}
