// AuthContext (scaffold placeholder).
// Carries: currentUser, accessToken/session, login(), logout(), updateProfile().
// Consumed by pages and RequireAuth/RequireAdmin — never fetches directly;
// all network calls live in src/services/auth.service.ts.
export {}
