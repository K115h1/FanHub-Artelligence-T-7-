// AdminDashboard page (scaffold placeholder) — route: /admin (RequireAdmin guard).
// Overview: usage statistics — active users, popular categories, view counts,
// feedback volume, chatbot interaction volume; quick links to management sections.
export {}
