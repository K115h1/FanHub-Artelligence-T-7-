// Explorer page (scaffold placeholder) — route: /explorer.
// Fandom Content Explorer: filters (category, genre, year, popularity, type),
// sorting (latest/popular/alphabetical), pagination, shareable URL query state.
export {}
