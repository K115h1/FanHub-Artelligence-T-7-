// ArticleDetail page (scaffold placeholder) — route: /articles/:id.
// Rich text with embedded images and timeline-style event highlights.
// SECURITY: sanitize any rich HTML before rendering (XSS defense) —
// prefer textContent/React escaping over dangerouslySetInnerHTML.
export {}
