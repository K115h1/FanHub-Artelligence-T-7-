// UI primitives (scaffold placeholder).
// shadcn-style components will live here, generated during the build phase:
// Button, Card, Input, Select, Dialog, Tabs, Badge, Avatar, DropdownMenu,
// Toast. Generated files are fully owned/customizable — no runtime dependency
// on external component libraries beyond their small utilities
// (clsx, tailwind-merge, class-variance-authority, lucide-react icons).
export {}
