// Footer — site footer (placeholder export for RootLayout).
// Real content (sitemap link, category shortcuts, feedback link, attribution)
// is built in the layout phase.
export default function Footer() {
  return <div data-slot="footer" />
}
