// Charts (scaffold placeholder) — lightweight SVG/CSS visualizations for the
// admin statistics page: popular categories, content view counts, feedback
// volume, chatbot interaction volume. Keep bundle small — no heavy chart library
// unless necessary.
export {}
