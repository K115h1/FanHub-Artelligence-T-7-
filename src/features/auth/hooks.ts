// Feature: Auth — business logic layer (scaffold placeholder).
// Planned hooks: useLogin(), useRegister(), useForgotPassword(), useResetPassword(),
// useSession(). These call src/services/auth.service.ts and update AuthContext.
// Pages that consume them: pages/auth/Login.tsx, Register.tsx,
// ForgotPassword.tsx, ResetPassword.tsx and pages/Profile.tsx.
export {}
