// Feature: Fan Submissions — business logic layer (scaffold placeholder).
// Planned hooks: useSubmitFanContent() (status = pending until admin approval),
// useMySubmissions().
// Consumed by pages/Articles.tsx and pages/Profile.tsx.
export {}
