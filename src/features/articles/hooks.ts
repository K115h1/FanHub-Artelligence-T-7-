// Feature: Characters & Articles — business logic layer (scaffold placeholder).
// Planned hooks: useCharacters() (card list with category filtering),
// useCharacter(id), useArticles() (rich text + embedded images),
// useEventHighlights() (timeline-style storytelling section).
// Consumed by pages/Characters.tsx, CharacterDetail.tsx, Articles.tsx, ArticleDetail.tsx.
export {}
