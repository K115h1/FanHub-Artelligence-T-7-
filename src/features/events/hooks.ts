// Feature: Events & Calendar — business logic layer (scaffold placeholder).
// Planned hooks: useEvents() (city/GPS/date filters), useCalendar() (month grid),
// uses hooks/useGeolocation.ts for proximity search.
// Consumed by pages/Events.tsx (map + calendar + ticket links).
export {}
