// Feature: Dashboard — business logic layer (scaffold placeholder).
// Planned hooks: useDashboardSummary() — greeting, recent activity,
// favorite fandoms, bookmarked items in as few requests as possible.
// Consumed by pages/Dashboard.tsx.
export {}
