// ThemeProvider (scaffold placeholder).
// Business logic: dark-mode toggle + font-size adjustment (SRS accessibility
// requirement), persisted to localStorage. Wraps ThemeContext from src/context/.
export {}
