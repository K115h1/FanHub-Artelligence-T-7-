// Route table (scaffold placeholder).
// Will define every page route with lazy loading:
//   / (Home + sitemap), /login, /register, /forgot-password, /reset-password,
//   /dashboard, /explorer, /content/:id, /characters, /characters/:id,
//   /articles, /articles/:id, /merchandise, /events, /bookmarks, /feedback,
//   /profile, /admin/*, * (NotFound)
// Protected routes are wrapped in RequireAuth / RequireAdmin (src/routes/).
export {}
