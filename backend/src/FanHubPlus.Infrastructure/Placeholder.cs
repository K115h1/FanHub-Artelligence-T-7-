// FanHubPlus.Infrastructure — Data layer (scaffold placeholder).
// Will contain: Data/AppDbContext.cs, Repositories/, Configurations/ (EF Core mappings).
// MySQL via Pomelo.EntityFrameworkCore.MySql.
// All SQL is parameterized (EF Core handles this) — never concatenate user input into queries.
