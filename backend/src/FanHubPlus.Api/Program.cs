// FanHubPlus.Api — Presentation layer (scaffold placeholder).
// Will contain: Program.cs, Controllers/, Middleware/, appsettings.json.
// Connection strings and secrets come from environment variables / user-secrets — never from committed files.
