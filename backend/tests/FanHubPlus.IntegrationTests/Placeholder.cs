// FanHubPlus.IntegrationTests — Integration test project (scaffold placeholder).
// Will contain end-to-end tests exercising controllers against a real/test database:
// register → login → browse → bookmark → feedback flows, plus error responses.
