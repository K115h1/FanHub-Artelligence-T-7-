# Diagrams — FanHub Plus

Required by the project report. Each file will be added during the documentation phase.

| File (planned)       | Diagram type | Contents                                                                 |
| -------------------- | ------------ | ------------------------------------------------------------------------ |
| `erd.png/svg`        | ERD          | All tables with PKs, FKs, and relationships (matches `database/01_schema.sql`) |
| `dfd-level0.png/svg` | DFD          | Context diagram: actors (Visitor, User, Admin) ↔ system ↔ external entities |
| `dfd-level1.png/svg` | DFD          | Level-1 DFD: auth, content, bookmark, feedback, admin data flows          |
| `flowcharts/`        | Flowcharts   | Visitor journey (landing → category → detail → bookmark) and admin content-management flow |
| `sitemap.png/svg`    | Sitemap      | Full site hierarchy — **must also appear on the Home page** (SRS requirement) |
| `architecture.png/svg` | Architecture | Frontend ↔ API layers ↔ MySQL deployment architecture                    |
